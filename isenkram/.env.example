# Isenkram Proxy Configuration

# Required: The destination URL to proxy requests to
ISENKRAM_DESTINATION=http://localhost:8080

# Optional: The address and port to bind the proxy server to
# Default: 127.0.0.1:3000
ISENKRAM_BIND=127.0.0.1:3000